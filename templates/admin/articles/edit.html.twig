{% extends 'admin/base_admin.html.twig' %}

{% block title %}Modifier l'article{% endblock %}

{% block body %}
<div class="container-flex py-5 mx-2">
  <div class="row">
    <div class="col-12 mb-3">
         <a  class="buttonStyleC btn btn-dark   " href="{{ path('articles_index') }}">retour à la liste</a>
     </div>
         <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h1  class =" text-center ttreT">Modifier l'article & news</h1>
                    </div>
                    <div class="card-body">
                        {{ include('admin/articles/_form.html.twig', {'button_label': 'Modifier'}) }}
                    </div>
                </div>
        </div>
        <div class="col-12 mt-3">
            {{ include('admin/articles/_delete_form.html.twig') }}
        </div>
    </div>
</div>

{% endblock %}
