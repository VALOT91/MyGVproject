{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
      {{ form_row(form.reference) }}
      {{ form_row(form.designation) }}
      {{ form_row(form.poids) }}
    <div style="display:flex;flex-direction:row;align-items:center">
        <img id="ConditionnementIm" style="margin-top:-1rem;margin-right:2rem; width:3rem; height:3rem;objective-fit:cover" src=" {{conditionnement.imagePath }}" class="   "  alt="...">   
        <span class="w-100">{{ form_row(form.image_path) }}</span>
    </div>
    <button class="btn btn-danger">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
