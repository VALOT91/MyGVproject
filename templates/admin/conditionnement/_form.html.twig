{{ form_start(form) }}

     
      {{ form_row(form.reference) }}
      {{ form_row(form.designation) }}
      {{ form_row(form.poids) }}

     
    <div style="display:flex;flex-direction:row;align-items:center">
        <img id="ConditionnementIm" style="margin-top:-1rem;margin-right:2rem; width:3rem; height:3rem;objective-fit:cover" src=" {{conditionnement.imagePath }}" class="   "  alt="...">   
        <span class="w-100">{{ form_row(form.image_path) }}</span>
         <button type="button" style="width:3rem;margin-top:1rem;margin-left:1rem;" class="btn btn-primary" id="opener">...</button>
    </div>
   
   <button class="btn btn-danger">{{ button_label|default('Enregistrer') }}</button>
  
{{ form_end(form) }}
 
  <div style="display:none" id="dialog" title="Sélectionnez une image">
    
        {% for key,item  in file %}
        <div  onclick="selectImageCond(this)" id= "dvIm{{ key }}"  class="my-2 imChoice " >
              <img id= "idIm{{ key }}"  onclick="selectImageCond(this)" style="height:3rem;" src="/{{ item.pathname}}" >
              <b><a href="#" onclick="selectImageCond(this)" id= "linkIm{{ key }}" style="height:max;text-decoration:none;font-size:small" >{{item.filename}}</a></b>
        </div>
        <hr>
        {% endfor %}

    </div>