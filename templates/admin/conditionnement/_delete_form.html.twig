<form method="post" action="{{ path('conditionnement_delete', {'id': conditionnement.id}) }}" onsubmit="return confirm('Voulez-vous vraiement supprimer ce conditionnement ?');">
 <div class="pos-button">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conditionnement.id) }}">
    <button  class="  btn text-white buttonStyleC btnBackEnr">Supprimer</button>
 </div>
</form>
