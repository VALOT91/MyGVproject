{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
      {{ form_row(form.nom) }}
      {{ form_row(form.description) }}
      
      <div style="display:flex;flex-direction:row;align-items:center">
        <img id="CategoryIm1" style="margin-top:-1rem;margin-right:2rem; width:3rem; height:3rem;objective-fit:cover" src=" \{{category.imagePath1 }}" class="   "  alt="...">   
        <span class="w-100">{{ form_row(form.imagepath1) }}</span>
          <button type="button"  id="opener1"  style="width:3rem;margin-top:1rem;margin-left:1rem;" class="btn btn-primary">...</button>

      </div>
       <div style="display:flex;flex-direction:row;align-items:center">
        <img id="CategoryIm2" style="margin-top:-1rem;margin-right:2rem; width:3rem; height:3rem;objective-fit:cover" src=" \{{category.imagePath2 }}" class="   "  alt="...">   
        <span class="w-100">{{ form_row(form.imagepath2) }}</span>
         <button type="button"  id="opener2"  style="width:3rem;margin-top:1rem;margin-left:1rem;" class="btn btn-primary">...</button>
      </div>
       <div style="display:flex;flex-direction:row;align-items:center">
        <img id="CategoryIm3" style="margin-top:-1rem;margin-right:2rem; width:3rem; height:3rem;objective-fit:cover" src=" \{{category.imagePath3 }}" class="   "  alt="...">   
        <span class="w-100">{{ form_row(form.imagepath3) }}</span>
         <button type="button"    id="opener3"  style="width:3rem;margin-top:1rem;margin-left:1rem;" class="btn btn-primary">...</button>
      </div>
  

 
     <button class="btn btn-warning">{{ button_label|default('Enregistrer') }}</button>

{{ form_end(form) }}
 <div style="display:none" id="dialog1" title="Sélectionnez une image">
    
        {% for key,item  in file %}
        <div  onclick="selectImageCat(this,1)" id= "dvIm{{ key }}"  class="my-2 imChoice " >
              <img id= "idIm{{ key }}"  onclick="selectImageCat(this,1)" style="height:3rem;" src="/{{ item.pathname}}" >
              <b><a href="#" onclick="selectImageCat(this,1)" id= "linkIm{{ key }}" style="height:max;text-decoration:none;font-size:small" >{{item.filename}}</a></b>
        </div>
        <hr>
        {% endfor %}

  </div>
  <div style="display:none" id="dialog2" title="Sélectionnez une image">
    
        {% for key,item  in file %}
          <div  onclick="selectImageCat(this,2)" id= "dvIm{{ key }}"  class="my-2 imChoice " >
                <img id= "idIm{{ key }}"  onclick="selectImageCat(this,2)" style="height:3rem;" src="/{{ item.pathname}}" >
                <b><a href="#" onclick="selectImageCat(this,2)" id= "linkIm{{ key }}" style="height:max;text-decoration:none;font-size:small" >{{item.filename}}</a></b>
          </div>
          <hr>
        {% endfor %}

  </div>
  <div style="display:none" id="dialog3" title="Sélectionnez une image">
    
        {% for key,item  in file %}
          <div  onclick="selectImageCat(this,3)" id= "dvIm{{ key }}"  class="my-2 imChoice " >
                <img id= "idIm{{ key }}"  onclick="selectImageCat(this,3)" style="height:3rem;" src="/{{ item.pathname}}" >
                <b><a href="#" onclick="selectImageCat(this,3)" id= "linkIm{{ key }}" style="height:max;text-decoration:none;font-size:small" >{{item.filename}}</a></b>
          </div>
          <hr>
        {% endfor %}

  </div>

