<form method="post" action="{{ path('tarif_delete', {'id': tarif.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
  <div class="pos-button">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarif.id) }}">
    <button class=" btn text-white buttonStyleC btnBackEnr">Supprimer</button>
  </div>
</form>
