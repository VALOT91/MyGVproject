{{ form_start(form) }}
  
    {{ form_row(form.reference) }}
    <div style="display:flex;flex-direction:row;align-items:center">
        <img id="produitConditionnementIm" style="margin-right:2rem;width:3rem; height:3rem;objective-fit:cover" src=" {{produit_conditionnement.imagePath }}" class="   "  alt="...">   
        <span class="w-100">{{ form_row(form.image_path) }}</span>
        <button type="button"  id="opener" style="width:3rem;margin-top:1rem;margin-left:1rem; " class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#ImgModal">...</button>  
    </div>

    
     {{ form_row(form.quantiteStock) }}
     {{ form_row(form.produit) }}
     {{ form_row(form.conditionnement) }}

     {# dialog box de selection des images #}
     {# {{ include('/_Image_Selection_Dialog.html.twig') }}     #}
   
    <button class="btn btn-danger">{{ button_label|default('Enregistrer') }}</button>

{{ form_end(form) }}
 <div style="display:none" id="dialog" title="Sélectionnez une image">
    
        {% for key,item  in file %}
        <div  onclick="selectImage(this)" id= "dvIm{{ key }}"  class="my-2 imChoice " >
              <img id= "idIm{{ key }}"  onclick="selectImage(this)" style="height:3rem;" src="/{{ item.pathname}}" >
              <b><a href="#" onclick="selectImage(this)" id= "linkIm{{ key }}" style="height:max;text-decoration:none;font-size:small" >{{item.filename}}</a></b>
        </div>
        <hr>
        {% endfor %}

    </div>

