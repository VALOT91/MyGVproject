
{# <img style=" width:3rem;objective-fit:cover" src=" {{produit_conditionnement.imagePath }}" class="   "  alt="..."> #}
{{ form_start(form) }}
    {{ form_row(form.reference) }}
    <div style="display:flex;flex-direction:row;align-items:center">
        <img id="produitConditionnementIm" style="margin-top:-1rem;margin-right:2rem; width:3rem; height:3rem;objective-fit:cover" src=" {{produit_conditionnement.imagePath }}" class="   "  alt="...">   
        <span class="w-100">{{ form_row(form.image_path) }}</span>
    </div>
    
     {{ form_row(form.quantiteStock) }}
     {{ form_row(form.produit) }}
     {{ form_row(form.conditionnement) }}
                  
    {# {{ form_widget(form) }} #}
    <button class="btn btn-danger">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
