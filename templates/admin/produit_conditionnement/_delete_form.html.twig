<form method="post" action="{{ path('produit_conditionnement_delete', {'id': produit_conditionnement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit_conditionnement.id) }}">
    <button class=" ms-2 btn btn-danger">Supprimer</button>
</form>
